plugins {
    id 'java'
    id "io.freefair.lombok" version "6.2.0"
    id 'org.gretty' version '3.0.7'
}

group 'org.example'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    implementation files('dependency/dependency-injection-v1.jar')
    implementation files('dependency/jaxrs-v1.jar')
    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.8.2'
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.8.2'
}

test {
    useJUnitPlatform()
}

gretty {
    contextPath = '/'
}
